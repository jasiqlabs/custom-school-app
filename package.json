{
  "name": "custom-school-app-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Custom School Management Web Application — Monorepo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "test:unit": "turbo run test:unit",
    "test:int": "turbo run test:int",
    "test:api": "turbo run test:api",
    "test:sec": "turbo run test:sec"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.5"
  }
}
