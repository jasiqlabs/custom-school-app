'use client';import Link from 'next/link';import { usePathname } from 'next/navigation';
export function SchoolNav({schoolId}:{schoolId:string}){const p=usePathname();const links=[['Overview',`/admin/schools/${schoolId}`],['Academics',`/admin/schools/${schoolId}/academics`],['Operators',`/admin/schools/${schoolId}/operators`],['Transfer Certificates',`/admin/schools/${schoolId}/transfer-certificate`]];return <nav className="row" aria-label="School sections" style={{marginBottom:16}}>{links.map(([n,h])=><Link key={h} href={h} className={p===h?'btn btn-primary':'btn btn-secondary'}>{n}</Link>)}</nav>}
