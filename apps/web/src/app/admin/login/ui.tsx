'use client';import { FormEvent,useState } from 'react';import { api } from '@/lib/api';
export default function AdminLoginForm(){const[err,setErr]=useState('');const[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setErr('');const f=new FormData(e.currentTarget);try{await api('/platform/auth/login',{method:'POST',body:JSON.stringify({email:f.get('email'),password:f.get('password')})});location.assign('/admin/dashboard')}catch(x:any){setErr(x.message)}finally{setBusy(false)}}return <main className="container" style={{paddingTop:80}}><form className="card grid" style={{maxWidth:430,margin:'0 auto'}} onSubmit={submit}><div><h1>Platform Admin</h1><p className="muted">Sign in to manage schools and official documents.</p></div><div className="field"><label htmlFor="email">Email</label><input id="email" name="email" type="email" autoComplete="username" required/></div><div className="field"><label htmlFor="password">Password</label><input id="password" name="password" type="password" autoComplete="current-password" required/></div>{err&&<div role="alert" className="error">{err}</div>}<button disabled={busy} className="btn btn-primary">{busy?'Signing in…':'Sign in'}</button></form></main>}
