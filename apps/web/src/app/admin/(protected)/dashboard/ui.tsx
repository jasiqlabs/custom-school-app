'use client';import { useEffect,useState } from 'react';import Link from 'next/link';import { api } from '@/lib/api';
export default function DashboardUi(){const[m,setM]=useState<any>();const[s,setS]=useState<any>();const[e,setE]=useState('');useEffect(()=>{Promise.all([api('/platform/dashboard/metrics'),api('/platform/dashboard/schools?pageSize=10')]).then(([a,b])=>{setM(a);setS(b)}).catch(x=>setE(x.message))},[]);return <div className="container grid"><div className="row" style={{justifyContent:'space-between'}}><div><h1>Platform Dashboard</h1><p className="muted">School portfolio overview without unnecessary student PII.</p></div><Link href="/admin/schools/new" className="btn btn-primary">Add School</Link></div>{e&&<div className="card error" role="alert">{e}</div>}<div className="grid grid-3">{[['Total',m?.totalSchools],['Active',m?.activeSchools],['Inactive',m?.inactiveSchools]].map(([k,v])=><div className="card" key={k}><div className="muted">{k} Schools</div><div style={{fontSize:32,fontWeight:800}}>{v??'—'}</div></div>)}</div><div className="card"><h2>Recent schools</h2><table><thead><tr><th>School</th><th>Status</th><th>Operators</th><th>Students</th><th>Last activity</th></tr></thead><tbody>{s?.items?.map((x:any)=><tr key={x.id}><td><Link href={`/admin/schools/${x.id}`}>{x.name}</Link><div className="muted" style={{fontSize:12}}>{x.id}</div></td><td><span className="badge">{x.status}</span></td><td>{x.operatorCount}</td><td>{x.studentCountAvailability==='AVAILABLE'?x.studentCount:'Unavailable until Student module'}</td><td>{new Date(x.lastActivityAt).toLocaleString()}</td></tr>)}</tbody></table></div></div>}
