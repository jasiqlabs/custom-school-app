'use client';import Link from 'next/link';import { FormEvent,useState } from 'react';import { api } from '@/lib/api';
export default function ResetUi(){const[msg,setMsg]=useState('');const[err,setErr]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setErr('');const email=new FormData(e.currentTarget).get('email');try{const r=await api<any>('/operator/auth/password-reset-request',{method:'POST',body:JSON.stringify({email})});setMsg(r.message)}catch(e:any){setErr(e.status===429?'Too many requests. Please try again later.':e.message)}}return <main className="container" style={{paddingTop:80}}><form className="card grid" style={{maxWidth:480,margin:'0 auto'}} onSubmit={submit}><h1>Password Assistance</h1><p className="muted">For security, the response is the same whether an account exists or not. In MVP, a Platform Admin completes the reset.</p><div className="field"><label>Email</label><input name="email" type="email" required/></div>{msg&&<div className="success" role="status">{msg}</div>}{err&&<div className="error" role="alert">{err}</div>}<button className="btn btn-primary">Request Assistance</button><Link href="/operator/login">Back to sign in</Link></form></main>}
