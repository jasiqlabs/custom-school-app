'use client';import Link from 'next/link';import { FormEvent,useState } from 'react';import { api } from '@/lib/api';
export default function OperatorLoginUi(){const[err,setErr]=useState('');const[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setErr('');const f=new FormData(e.currentTarget);try{await api('/operator/auth/login',{method:'POST',body:JSON.stringify({email:f.get('email'),password:f.get('password')})});location.assign('/operator')}catch(e:any){setErr(e.message)}finally{setBusy(false)}}return <main className="container" style={{paddingTop:80}}><form className="card grid" style={{maxWidth:430,margin:'0 auto'}} onSubmit={submit}><div><h1>School Operator</h1><p className="muted">Sign in to your assigned school.</p></div><div className="field"><label>Email</label><input name="email" type="email" autoComplete="username" required/></div><div className="field"><label>Password</label><input name="password" type="password" autoComplete="current-password" required/></div>{err&&<div className="error" role="alert">{err}</div>}<button className="btn btn-primary" disabled={busy}>{busy?'Signing in…':'Sign in'}</button><Link href="/operator/forgot-password">Forgot password?</Link></form></main>}
